{
  "name": "karly",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "all": "run-s serve start",
    "serve": "npx json-server server/db/data.json",
    "start": "npx live-server client --host=localhost --port=3000 --no-browser"
  },
  "devDependencies": {
    "json-server": "^0.17.1",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "swiper": "^8.4.6"
  }
}
