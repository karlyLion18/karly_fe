{
  "name": "karly",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "all": "run-p serve start",
    "serve": "npx json-server server/db/data.json",
    "start": "npx live-server client --host=localhost --port=3000 --no-browser"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "json-server": "^0.17.1",
    "live-server": "^1.2.2"
  }
}
