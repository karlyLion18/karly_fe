{
  "name": "karly",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "all": "run-p serve_product serve_users start", 
    "serve_product": "npx json-server server/db/data.json --port=5001",
    "serve_users": "npx json-server server/db/users.json --port=5002",
    "start": "npx live-server client --host=localhost --port=8080 --no-browser"
  },
  "devDependencies": {
    "json-server": "^0.17.1",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "swiper": "^8.4.6"
  }
}
